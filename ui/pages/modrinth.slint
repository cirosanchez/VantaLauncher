import { Theme, Page } from "../theme.slint";

export component ModrinthPage inherits Rectangle {

    border-color: Theme.accent_3;
    border-radius: 12px;
    border-width: 2px;
    background: Theme.accent_2;


    Text {
        text: "modrinth";
    }
}
