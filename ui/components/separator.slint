export component Separator {
    in property <length> thickness: 1px;
    in property <color> separator_color;

    height: thickness;

    Rectangle {
        width: parent.width;
        height: parent.height;
        background: separator_color;
    }
}
